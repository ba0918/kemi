# 用語集

このリポジトリで二通りに読める言葉の意味を固定する。一般語を一般の意味で使う
語は載せない。

## kemi
このツールの名前。リポジトリ名とバイナリ名。「閲する（けみする）」から。
「diff-review」は旧ツール（Python）を指す。新ツールを diff-review と呼ばない。

## レビュー
kemi が 1 回の起動で提示する変更のまとまり、およびそれを人間が読んで
判断する行為。1 回の起動が 1 レビュー。

## 入力モード
レビューの元データの与え方。manifest / コミット範囲 / worktree / staged の 4 つ。

## manifest
入力モードの 1 つ。グループ、差分、承認対象を JSON で与えるもの。
旧 `diff-review` の manifest と互換。

## グループ
レビュー内の変更のまとまり。コミット範囲では、グループ単位が最終形なら範囲全体で
1 つ、コミットごとなら 1 コミット。manifest では作成者が決めた単位。
`title`（何を変えたか）と `why`（なぜ変えたか）、`watch`（見てほしい点）を持つ。

## グループ単位
コミット範囲でのグループの組み方。「最終形」（範囲全体を 1 グループにし、各ファイルを
最終形で 1 回だけ出す。`--group-by file`）と「コミットごと」（マージ以外の 1 コミットを
1 グループにする。`--group-by commit`）の 2 つがあり、画面で切り替える。
1 列 / 2 列の切り替え（表示モード）と混同しない。「表示モード」と呼ばない。

## 表示モード
差分を 1 列（unified）で出すか 2 列（split）で出すか。グループ単位のことではない。

## 変更ブロック
差分の中で、追加・削除・書き換えの行が途切れずに続くまとまり。前後の同じ行
（文脈の行）は含まない。折りたたみで区切られた範囲（ハンク）とは別で、1 つの
ハンクに複数の変更ブロックが入りうる。「次の変更」の移動はこの単位で止まる。

## 由来
最終形の変更ブロックに示す、その行を変えたコミット（またはマージ）。変更ブロックの
すぐ上の 1 行（由来の行）に出す。コメントと混ざるので「注記」「注釈」と呼ばない。

## ファイルエントリ
グループの中の 1 ファイル。パス、状態、増減数、行データを持つ。

## コメント
人間がファイルの行レンジまたはファイル全体に付ける書き込み。返信を持てる。
「注釈」「アノテーション」とは言わない。

## スレッド
1 つのコメントとその返信のまとまり。

## 解決
コメントを「対応済み」として閉じること（resolve）。コメントは消えない。
「削除」と混同しない。

## ファイル単位コメント
行を持たないコメント。`start_line` / `end_line` が `null`。

## suggestion
コメントに付随する、新側の行レンジを置換する文字列。空文字は行の削除。
挿入のみは隣接行を含む置換で表す。自由文の提案とは区別する。

## 適用
suggestion を作業ツリーのファイルに書き込むこと。kemi は行わない。
エージェントが行う。

## quote
コメント作成時の対象行のテキスト。ファイルが変わっても位置を特定できる
ようにするためのもの。

## outdated（古いコメント）
コメント作成時以降に対象ファイルの内容が変わったコメント。表示は変えるが、
行番号の自動付け替えはしない。

## submit
レビューを終える操作。「承認」か「変更要求」の verdict を伴う。

## verdict
submit で示す判断。`approved`（承認）か `changes_requested`（変更要求）。

## digest
`--digest` が出す、レビューの地図。行内容を含まない有界な JSON。
「サマリ」とは言わない（要約ではなく機械的な集計のため）。

## ノイズ
lockfile、生成物、vendored、minified など、機械的にそうと分かる変更。
重要度の推定ではない。畳んでも印は残る。

## focus / note / 重要
focus はエージェントが付ける「優先して見てほしい」の印。note はその理由。
重要は focus が付いた状態を指す。kemi 自身は重要を推定しない。

## 更新バッジ
ライブリロードで変更を検知したときに出す印。押すと再取得する。

## 見た（閲覧済み）
人が「このファイルは読んだ」と印を付けた状態。ページ上の印であってレビューの判断
ではない。サーバのメモリにセッション状態として持つ。ファイルエントリごとに持つので、最終形とコミットごとでは
同じパスでも別に付く。

## 結果ファイル
submit で stdout に出した JSON と同じ内容を、利用者の状態ディレクトリに残した
ファイル。`kemi --result` で読む。セッション状態（メモリ）とは別物で、レビュー中の
コメントや見たは保存しない。

## 下書き
入力中で、まだコメントとして送っていない本文。ブラウザの localStorage に残る。

## identity
manifest の `approval` が与える「承認対象を識別する値」（例 `sha256:...`）。
kemi は画面に出さず、検証もしない。結果の JSON にそのまま返す。

## unix
このリポジトリの文書（仕様、README、コードのコメント）で「Linux と macOS」を
指す語。Windows の対義。POSIX 全体やそれ以外の OS を指す一般の意味では使わない。
